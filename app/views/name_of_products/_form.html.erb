<%= form_with(model: name_of_product) do |form| %>
  <% if name_of_product.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(name_of_product.errors.count, "error") %> prohibited this name_of_product from being saved:</h2>

      <ul>
        <% name_of_product.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>

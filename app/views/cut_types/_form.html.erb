<%= form_with(model: cut_type) do |form| %>
  <% if cut_type.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(cut_type.errors.count, "error") %> prohibited this cut_type from being saved:</h2>

      <ul>
        <% cut_type.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
